The three separate files (.py) are the correct ones that should be used in the files for the website representation!
We also managed to implement the code from the recommendation algorithms into the website, but it's not on the presentation video because it was a last minute save!
In data.py raw 473: len= len(list) is now len= len(eventRecommendations). 
